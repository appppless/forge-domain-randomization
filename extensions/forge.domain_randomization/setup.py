from setuptools import find_packages, setup


setup(
    name="forge-domain-randomization",
    version="0.1.0",
    description="USD-layer based domain randomization extension for FORGE.",
    packages=find_packages(),
    python_requires=">=3.10",
)
